# 実装・公開の一括確認記録

実施日: 2026-09-11 JST（API時刻はUTC）。対象仕様は別Workerの最終精査 `ddd765b`。実装コミットは `0d42ee9`（API・D1・CSV）と `d8ae7c8`（画面・再開・公開設定・統合試験）です。Subworkerは使用せず親エージェントが実装しました。

## 環境と確認範囲

- macOS 15.6 (24G84)、Apple Silicon、Devbox 0.18.0、Node 24.12.0。
- Chrome 153.0.8010.36。通常1280×1000、キーボードのみ1024×1000、小画面案内800×900を確認。
- React 19.3.0、jsPsych 8.3.0、survey-likert 2.2.0。React StrictModeを有効にした開発サーバーで試験。
- ローカルD1、別途 `getPlatformProxy({persist:false,remoteBindings:false})` による分離した実D1 binding、合成データ用の公開D1を使用。DB全体をモックして合格にはしていません。
- 通信障害はChromeのルーティングで再現。API・DBの同時送信、4件目の完了更新失敗、削除失敗は実D1で再現。

## 一括結果

| 対象 | 操作と結果 | 該当基準 |
| --- | --- | --- |
| 環境・ビルド | 既存依存・DB・認証ファイルなしの `/private/tmp/jspsych-fresh.dYtKXj` にソースを複製し、Devbox setup/check/build、5175番での起動とChrome4回答完遂が成功。これは作業ツリーの分離再現であり、未コミット変更を含むgit cloneそのものではない。最終成果物に管理認証変数・DB/アカウントIDの混入がないことも確認 | AC01 |
| 刺激・割り当て | `tests/study.test.ts` で乱数0、0.499999、0.5、0.999999の分岐、共通4場面・同一群・共通提案、前置き各1文の敬体を確認。全文は `shared/study.ts` に固定し目視確認。文字数の同等性や心理的効果を証明したとは扱わない | AC02・03 |
| 作成・入力・認証 | 同じ開始4並行で201が1件、200が3件、manifest・期限一致。未同意・旧版・余分なキー・不正尺度を拒否。受付停止は新規だけ403、既存は再開・保存可。不正tokenと未知IDは同じ401 | AC04・05 |
| 2尺度と端末保存 | 未選択・片方だけでは進まない。値1・7が保存される。端末保存を失敗させても選択値と場面を保持。ID保存に失敗した場合POSTを送らない | AC06・07 |
| 再送・完了 | 同じ回答4並行で1行。異なる回答は409で不変。3件は未完了、4件で完了。完了更新をトリガーで失敗させると4件目のINSERTもロールバック。同一再送で完了時刻不変 | AC08・09 |
| 通信・手動操作 | 503・通信切断・15秒待ち・429を再現。DB保存確認までは次場面を表示しない。復旧だけでは再送しない。429の待機指定中は通常再開・再送を無効化。手動再送後に完了 | AC10 |
| 再開 | 最初の回答前、途中場面、確定して未送信、作成応答消失、最終保存応答消失、タブ閉鎖・再読み込みを確認。API照合後に未送信分を手動再送し、次場面へ進む | AC11 |
| 再開メタデータ | 最初の回答前の再提示はresumeCount=1、presentationAttempt=2。課題再開では回数を加算。全回答確定後の保存確認だけではDTOが不変 | AC12 |
| 排他 | 2タブ目は案内のみで同じID・保存キーを変更しない。Web Locks不足は開始不可。排他はID作成・進捗消去を含むコントローラー全体を保護 | AC13 |
| 期限 | 分離D1で24時間の直前は200、一致・直後は410。不正認証は期限後も401。30日の直前のみ取得対象。完了状態は24時間後も保持。端末時計が進む・遅れる場合を確認 | AC14 |
| 完了確認 | 4回答のサーバー保存確認後だけお礼を表示し、端末の回答・token・送信待ちを消す。最終応答消失後はGETで確認して完了 | AC15 |
| CSV | 完了試行・未完了試行・全セッション・メタデータを固定出力。提示文・質問文全文、1〜7尺度、0回答を含める。token/hashを除外。空CSVのヘッダー、引用符・改行エスケープを確認 | AC16・17 |
| 削除 | ローカルD1で30日の直前は残存、一致以降は取得除外・連動削除。削除失敗トリガーでロールバック、解除後の再実行で親子行削除。復元試験は対象外 | AC18 |
| UI | 通常PC幅、キーボードのみの4場面完遂、小画面案内、必須機能の案内、StrictModeで二重開始・入力消失なし。公開画面のスクリーンショットも目視確認 | AC19・20 |
| 公開 | ローカルの5173・5175番サーバーを停止後、合成用公開先で正常完遂・通常再開・時計ずれ回帰が成功。公開APIで4回答・同じmanifest・完了を確認し、CSVで照合。Cron `17 * * * *` のデプロイ設定を確認 | AC21 |

必須動作を複数の試験でまとめて確認しており、各ACを独立した1テストにしているわけではありません。公開Cronの実際の成功は未確認で、設定確認と区別しています。実参加者による予備実施、研究上の尺度の妥当性・効果、同時利用性能は未検証です。

コミット後の追加確認として、`d8ae7c8` を `/private/tmp/jspsych-checkout.yGeIFv` へ `git clone --no-hardlinks` し、実際の新規チェックアウトで `devbox run setup`・`devbox run check`・`devbox run build` がすべて成功しました。新規ローカルDBへの2マイグレーション、8試験の成功、公開版と同じブラウザー成果物 `index-CrzaQsYs.js` を確認しました。README・docsのローカルリンク先と差分の空白チェックも成功しています。

## 実行コマンドと証拠

```sh
devbox run check
devbox run -- npm run test:api
devbox run -- npm run test:e2e
devbox run -- npm run test:volume
devbox run -- env TEST_BASE_URL=https://jspsych-demo-staging.jspsych-demo.workers.dev npx playwright test --grep 'normal resume completes|required scales'
devbox run -- npm run data:export -- --target demo --output exports/public-verification-20260911
```

ローカルAPI統合試験で作成した合成ID例: `6b4f7dd7-332c-462b-856a-a21a3a30c47e`。同時作成・同時PUT・衝突・ロールバック・期限後拒否が成功しました。

初回公開通し確認のID: `743150e9-5420-4585-bec6-7de7b53bd6e8`。完了時刻 `2026-09-10T18:56:27.021Z`、4回答、理解感1・有用性7、ブラウザーのpageerrorは0件。公開CSVは2完了セッション・8試行、未完了試行0行（ヘッダーあり）でした。

公開CSVのファイルは `exports/public-verification-20260911.*`、画面の例は `test-results/study-normal-resume-completes-with-server-confirmed-values/task.png`。いずれも合成データでGit対象外です。

最終公開版は `0704842d-83ab-424f-888a-be709a66141b`。この版でローカルサーバー停止後の公開Chrome試験3件が成功しました。再開完了のIDは `53357c1e-786e-4c67-bf66-09a9c41045af`、完了時刻 `2026-09-10T19:05:26.605Z`。`exports/public-final-20260911.*` は全5セッション（4完了・1件は0回答）、完了試行16行、未完了試行0行です。

最終ローカル結果は、型チェック成功、単体・分離D1試験8件成功、Chrome13件の通し試験に加えて動作中の期限到達1件成功（計14件）です。時計ずれ修正後の `src/session.ts` SHA-256は `51236deb05a246c1a4d3a89cfc7cb78e08b67bb1e6a006c68d2e0647d736cc90`。最終の主要ファイルに `git diff --check` の問題はありません。

500件試験は要件の簡略化と並行して既に作業中だった追加検証です。`/var/folders/ny/6lsmzt1d1fngy874bk6df87m0000gn/T/jspsych-volume-Q80FdF/` に4ファイルの記録を残しました。テスト用接頭辞 `volume-28204400-c6ac-4411-9841-c51fee01aeee-` の500完了セッション・2,000完了試行を全件照合し、既存の他の合成行とは分けて数えました。これはローカルの固定データでの整合性確認だけで、公開500名や同時500名を保証しません。

最終の再実行では期限切れ未完了を2回答の例に揃え、`/var/folders/ny/6lsmzt1d1fngy874bk6df87m0000gn/T/jspsych-volume-hNEDp2/` に記録しました。接頭辞 `volume-6f3ca83f-bdee-45f1-a4db-32b2747f11e5-` の505保持中セッション（500完了＋0〜3回答と期限切れ未完了）、完了2,000試行・未完了8試行を照合しました。30日到達の2セッションは取得されず、その後の削除失敗・再実行・CASCADEも成功しました。

## Reviewerの指摘と判断

ユーザー指定の `reviewer`、GPT-6 Astra、mediumを使用し、文脈を継承せず依頼文・対象仕様・範囲を渡しました。mainへのマージ可否と公開運用可否を厳しく確認するよう依頼し、実参加者の研究準備と技術デモの完成を分離しました。

| 回 | 指摘 | 採否と対応 |
| --- | --- | --- |
| 1 | 再開GET失敗後に通常再開ボタンを再度押すとpausedが残り、4回答後も送信されない。429待機も迂回できる | P1として採用。失敗中の通常再開を無効化し、手動retry経路へ統一。429後から完了までの回帰試験を追加 |
| 1 | 1024px未満の操作非表示が最新の「固定幅で禁止しない」要件に反する | P2として採用。幅によるhiddenを撤廃し、PC案内だけに変更。小画面でも同意欄が表示されることを確認 |
| 2 | 端末のDate.nowだけで期限判定すると、時計が進んだ端末で有効な回答とtokenを消してしまう | P1として採用。端末壁時計だけの消去を撤廃し、serverTimeと実行中の単調時計を使用。進んだ時計で同一IDを保持して再開する回帰試験を追加 |

初回と2回目の指摘はいずれも具体的な再現があり、既存契約の正しさを回復するものと判断しました。過剰な指摘として却下したものはありません。構造の好みや実参加者募集に必要な追加機能を、本実装の必須修正には取り込んでいません。

3回目の最終レビューで、指摘3件の解消、現在のソースと記録のハッシュ一致、公開CSVの5セッション・16完了試行・未完了CSVのヘッダー、公開確認IDの4試行と1セッションの対応が確認されました。Reviewerはmainへのマージと合成データ専用の技術デモとしての公開完成を承認しました。全試験の独立再実行ではなく、コード・試験内容・保存成果物と親の実行記録を照合したレビューです。

2026-09-11 JST、親エージェントは、3回のレビューで挙がった具体的不具合が全て解消し、回帰試験・公開の正常系と通常再開・CSV照合が揃ったことを根拠に、技術デモの実装作業を終了と判断しました。公開Cronの実際の成功確認と実参加者の研究準備は、合意済みの運用事項として残します。これらを実施済み・承認済みとは扱いません。
